---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CrosswordPageBody from '../../components/page/CrosswordPageBody.astro';
import { useT, getLocale } from '../../i18n';

const locale = getLocale(Astro);
const t = useT(locale);
---

<BaseLayout title={t.pageMeta.crosswordTitle} description={t.pageMeta.crosswordDescription}>
  <CrosswordPageBody />
</BaseLayout>
