---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import GamesPageBody from '../../../components/page/GamesPageBody.astro';
import { useT, getLocale } from '../../../i18n';

const locale = getLocale(Astro);
const t = useT(locale);
---

<BaseLayout title={t.pageMeta.gamesTitle} description={t.pageMeta.gamesDescription}>
  <GamesPageBody />
</BaseLayout>
